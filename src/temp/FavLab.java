package temp;
/*package com.lightpainting.app;

import java.util.ArrayList;
import java.util.UUID;

import android.content.Context;

public class FavLab {

	private static ArrayList<FavItem> mFavs;
	
	private static FavLab sFavLab;
	private Context mAppContext;
	
	private FavLab(Context appContext){
		
		mAppContext = appContext;
		mFavs = new ArrayList<FavItem>();
					
		}
		
		public static FavLab get(Context c){
			if(sFavLab == null)
			{
				sFavLab = new FavLab(c.getApplicationContext());
			}
			return sFavLab;
			
			
			}
		public ArrayList<FavItem> getFavs(){
			return mFavs;
			}
		
		public FavItem getFavItem(UUID id){
			for(FavItem f:mFavs){
				if(f.getId().equals(id))
					return f;
			}
			return null;
		}
		
	
}
*/


